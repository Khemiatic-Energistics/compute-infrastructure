# Deployment for the Docking Job Controller
apiVersion: apps/v1
kind: Deployment
metadata:
  name: docking-controller
  namespace: default
  labels:
    app: docking-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: docking-controller
  template:
    metadata:
      labels:
        app: docking-controller
    spec:
      serviceAccountName: docking-controller
      containers:
        - name: controller
          image: docking-controller:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          env:
            - name: NAMESPACE
              value: "default"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
---
# Service for the Docking Job Controller (cluster-internal access only)
apiVersion: v1
kind: Service
metadata:
  name: docking-controller
  namespace: default
  labels:
    app: docking-controller
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: docking-controller
